@if (ribbons == null)
{
    <h3 class="mat-subtitle1">Loading your ribbons</h3>
    <MudProgressCircular Color="Color.Success" Indeterminate="true" />
}
else
{
    <div class="bg-white shadow overflow-hidden sm:rounded-lg">
        <div class="bg-white">
            <div class="mx-auto py-12 px-4 max-w-7xl sm:px-6 lg:px-8 lg:py-24">
                <div class="space-y-12">
                    <div class="space-y-5 sm:space-y-4 md:max-w-xl lg:max-w-3xl xl:max-w-none">
                        <h2 class="text-3xl font-extrabold tracking-tight sm:text-4xl">Your Ribbons</h2>
                    </div>
                    <ul role="list"
                        class="space-y-12 sm:grid sm:grid-cols-2 sm:gap-x-6 sm:gap-y-12 sm:space-y-0 lg:grid-cols-3 lg:gap-x-8">
                        @foreach (var ribbon in ribbons)
                        {
                            <li>
                                <div class="space-y-4">
                                    <div class="aspect-w-3 aspect-h-2">
                                        <a href="@("/ribbon/" + ribbon.id)">
                                            <img class="object-cover shadow-lg rounded-lg"
                                                 src="@(ribbon.path)" width="80"
                                                 alt="">
                                        </a>
                                    </div>

                                    <div class="space-y-2">
                                        <div class="text-lg leading-6 font-medium space-y-1">
                                            <h3>@(ribbon.name)</h3>
                                            <p class="text-indigo-600">@(ribbon.description)</p>
                                        </div>
                                    </div>
                                </div>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </div>
    </div>
}

@code {
    [Parameter]
    public IEnumerable<Ribbon>? ribbons { get; set; }

    private bool isVisible;

    public void ToggleOverlay(bool value)
    {
        isVisible = value;
    }
}
